#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <stdint.h>
#include <string.h>

int boyer_moore(char*, char*);
int length(char*);
char *substr(char *, int, int); 
char *sort(char *);
char *concat(char *, char *);